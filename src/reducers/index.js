const initialState = {
  film_id: null,
  title: null,
  overview: null,
  poster_path: null,
  films: [],
  searchFilms: [],
};

const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
